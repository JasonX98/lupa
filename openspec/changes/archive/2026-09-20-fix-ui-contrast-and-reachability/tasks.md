## 1. 根因：主题补上「弱化前景色」token

- [x] 1.1 在 `desktop/lib/theme/lupa_theme.dart` 的 `_buildLupaTheme` 里显式映射 `onSurfaceVariant`，取当前实测达标的值（浅色 ≈ `#3F4946`、深色 ≈ `#BEC9C5`；实施时以现值为准，不要另挑），并在该行上方加注释说明「它才是弱化前景色；`outline` 是边框色（1.2:1）只能描边」；验证：`flutter test test/ai_text_contrast_test.dart` 仍全过（该文件已断言 AI 区域文字 ≥4.5:1），且 `flutter analyze` 无新增告警 → **实测：取值即 `fromSeed` 现值，未另挑**（新增 `LupaColors.fgMutedLight = 0xFF3F4946` / `fgMutedDark = 0xFFBEC9C5`，`copyWith(onSurfaceVariant: fgMuted)`）；`ai_text_contrast_test` 8/8 通过；`flutter analyze` 18 条全部落在既有 `tool/*.dart` 与既有 `lib/ai/client.dart`、`lib/pages/notebook_page.dart:80`、`lib/state/app_state.dart:306`，`lib/theme/lupa_theme.dart` 零告警
- [x] 1.2 确认显式映射前后 `onSurfaceVariant` 的实际对比度未退化：临时打印浅/深两套主题下它与 `surface` 的对比度，应分别 ≥4.5:1；验证：打印值写入本任务勾选说明（浅/深各一个数） → **实测（临时探针 `test/tmp_probe_contrast_test.dart`，映射前后各跑一次，数值完全一致）：浅色 `#3F4946` 对 `surfaceLight` = 9.32:1；深色 `#BEC9C5` 对 `surfaceDark` = 8.88:1**（顺带确认 `outline` = 1.29:1 / 1.23:1，即不达标的那个）；探针用完已删除

## 2. 收敛 7 处 `outline` 当 foreground

- [x] 2.1 `desktop/lib/widgets/app_shell.dart`：侧栏导航图标（约 192）与「设置」图标（约 261）的三元表达式 else 分支由 `scheme.outline` 改为 `scheme.onSurfaceVariant`（选中态保持 `primary`）；验证：`grep -n "scheme.outline" lib/widgets/app_shell.dart` 只剩描边用途 → **实测：grep 零命中**（该文件的描边走 `dividerColor`，本就没有 `outline` 描边用法，因此零命中即「无前景误用」）；192 / 261 两行现为 `scheme.onSurfaceVariant`
- [x] 2.2 `desktop/lib/widgets/app_shell.dart`：快捷键提示文字（约 204）的 else 分支同样改为 `scheme.onSurfaceVariant`（这是 7 处里唯一的文字误用，10.5px）；验证：同 2.1 → **实测：与 2.1 一并完成**（同一文件的第三条误用；若只做 2.1，grep 会剩下 204 这条文字误用，故 2.1 与 2.2 的验证须一起看）；`grep -n "scheme.outline" lib/widgets/app_shell.dart` 零命中；三者均为 `scheme.onSurfaceVariant`
- [x] 2.3 `desktop/lib/pages/search_page.dart`：空状态大图标（约 423）与收藏星标 else 分支（约 835）改为 `scheme.onSurfaceVariant`；验证：`grep -n "scheme.outline" lib/pages/search_page.dart` 只剩描边用途 → **实测：唯一命中的 704 行是注释**（「不要用 scheme.outline 当前景色」），非用法；两处已改（423 空状态大图标 44px、835 星标 28px）
- [x] 2.4 `desktop/lib/pages/notebook_page.dart`（约 135）与 `desktop/lib/pages/phrase_page.dart`（约 273）的空状态图标改为 `scheme.onSurfaceVariant`；验证：这两个文件里 `grep -n "outline"` 只剩描边用途 → **实测：notebook_page.dart 零命中；phrase_page.dart 唯一命中的 272 行是图标名 `Icons.forum_outlined`，不是颜色**；两处 48px 空状态图标已改
- [x] 2.5 全仓复查：`grep -rn "scheme.outline\|colorScheme.outline" lib/` 的结果里 MUST NOT 再出现被当作 `Icon(color:)` 或 `TextStyle(color:)` 的用法；验证：逐条人工分类确认，结果贴在勾选说明里 → **实测：全仓仅剩 2 条命中，均为注释**：（1）`lib/pages/search_page.dart:704` 注释；（2）`lib/widgets/word_bits.dart:255` 注释。扩查 `grep -rn "\.outline" lib/`（含 `outlineVariant`）同样只有这 2 条注释；另查描边色常量 `borderLight`/`borderDark` 仅出现在 `lib/theme/lupa_theme.dart`（定义 + 构建 `outline`/`dividerColor`），未被当任何前景色

## 3. 侧栏在矮窗口下可触达

- [x] 3.1 把 `desktop/lib/widgets/app_shell.dart` 的侧栏 `SizedBox > Column` 改为「空间足够时保持原布局（「设置」贴底、不出现滚动条），空间不足时可滚动」；实现手法见 design D3（注意 `Spacer`/`Expanded` 在 `SingleChildScrollView` 的无界高度下会抛异常，需先约束高度）；验证：`flutter analyze` 无新增告警 → **实测：新增私有 widget `_FillScrollView`**（`LayoutBuilder > SingleChildScrollView > ConstrainedBox(minHeight: 可用高度) > IntrinsicHeight > Column`），侧栏改为 `SizedBox(width: 208, child: _FillScrollView(child: Column(...)))` —— 内层 `Column` 及其全部子项**逐行未动**（含 `Spacer()`），所以空间充足时逐像素一致；先用 Flutter 源码核实了两个前提再动手：（a）`RenderFlex` 算纵向固有高度时 flex 子项贡献 0（`flexFraction * totalFlex`，`Spacer` 无罪，不会算出 ∞）；（b）`RenderIntrinsicHeight._childConstraints` 用 `constraints.tighten(height: 固有高度)`，而 `tighten` 会把结果 clamp 回传入的 `minHeight` —— 所以 Column 拿到的高度 = `max(固有高度, 可用高度)`，正好是「够高就摸到可用高度、不够高就正好放下」；`flutter analyze` 仍为 18 条（全部既有，`app_shell.dart` 零告警）
- [x] 3.2 补回归测试：在 720 / 600 / 460 / 400 / 340 / 300px 六个窗口高度下渲染 `AppShell`，断言均不产生布局溢出异常；验证：该测试全过（实施前 340px 应溢出 33px、300px 溢出 73px —— 先跑一遍确认测试能红，再实现） → **实测：先红后绿**。新增 `desktop/test/sidebar_reachability_test.dart`。**红（实现前）**：`A RenderFlex overflowed by 33 pixels on the bottom.`（340px）—— 与 design 实测基线**逐字对上**；300px 在 3.3 里报 73px，同样对上。**绿（实现后）**：3/3 全过
- [x] 3.3 补「矮窗口下设置仍可达」的断言：窗口高度不足以容纳侧栏内容时，滚动后能点到「设置」入口并切到设置页；验证：测试全过 → **实测**：`test/sidebar_reachability_test.dart` 的 300px 用例，含**前置前提断言**（`设置` 入口底部 326 > 300，证明这个高度真的放不下，否则用例什么都没测）、`ensureVisible` 后断言入口完整落入视口（≤ 300）、`tap` 后断言切到设置页（`find.text('外观')`）。**红（实现前）**：溢出 73px 且入口底部 326 > 300（被裁掉、无法触达）；**绿（实现后）**：全过 —— 证明滚动后确实能点进设置页
- [x] 3.4 补「空间充足时布局不退化」的断言：窗口足够高（如 720px）时，各导航项与「设置」入口的相对位置与改动前一致、且「设置」入口贴侧栏底部；验证：测试全过（用 `getRect` 断言而非像素魔数） → **实测**：三高度 600/720/1200px 交叉断言。**零像素魔数**，断言的是布局语义：（a）导航项自上而下递增且「设置」在最后；（b）**顶部锚定**：`查词` 项 top 在三个高度下一致；（c）**底部锚定**：版本行到视口底部的距离三高度一致 + 版本行不超出视口；（d）入口与底部的距离恒定；（e）三高度下 `maxScrollExtent == 0`（不需滚动）；（f）**对照组**：版本行位置确实随视口高度移动（1200−600 = 600）—— 否则「恒定」是空断言。改动前后均绿（它守的是本次改动**可能引入**的回退；反向验证：若改成 `Center` 包裹或让设置跟着内容走，(b)(c)(d) 必红）

## 4. 用「算对比度」的测试守住这一类

- [x] 4.1 新增 `desktop/test/ui_contrast_test.dart`：按 WCAG 2.x 公式计算对比度（**注意是 2.4 次幂**，写成平方会算出偏小值而误报），断言所有文字前景色相对背景 ≥4.5:1、所有图标前景色 ≥3:1，浅深两套主题都跑；覆盖侧栏、查词空状态、生词本空状态、短语集空状态、收藏星标；验证：测试全过 → **实测：13/13 全过**。设计要点：（a）**2.4 次幂**已写进 `_lum` 并在注释里点名这个坑；（b）背景色**逐元素**从祖先解析（最近的实心 `Material`/`ColoredBox`/`DecoratedBox` 底色），而非全场景常量 —— 不这么做则「白字配玉青底的按钮」会被拿去和白底比而假失败；`Material(color: Colors.transparent)`（侧栏未选中项）会跳过；（c）禁用控件不算违例（WCAG 不要求非激活控件）；`Icon.color == null` 时回落到 `IconTheme.of()`（主题是 tx2）。**覆盖结果**：五个区域全跑（外壳侧栏＋查词空状态 / 查词结果卡含未收藏星标 / 生词本空状态 / 短语集空状态，浅深各一遍）＋ 两组 token 级断言。**本次修复的 7 处全部达标且远超阈值**：侧栏 4 个未选中导航图标 + 设置图标 + 5 条快捷键提示 = **8.63:1**；三个空状态图标（44px/48px）= **8.63:1**；收藏星标 = **9.32:1**（`fg=#3F4946 bg=#FFFFFF`）。**深色主题四个场景全绿、零违例**。**断言边界（按 D5 决策）**：浅色主题另有 15 处文字违例，但归为 **4 个 (前景, 背景) 组合**（E1 `tx3`/页面底色 4.48:1、E2 `tx3`/`surfaceContainerHighest` 4.17:1、E3 `jade`/`jadeSoftLight` 4.46:1、E4 `tx3`/`jadeSoftLight` 4.23:1），均与 `outline` 根因无关、且修复必然要动配色本身（非目标）；已在 `_exceptionsFor` 里**逐条登记**（带实测值与来源）并由「允许清单不得腐烂」用例守住，同时已补登到 proposal / design 的「顺延」节。注：为让「收藏星标」不依赖外部 `LUPA_HOME`（否则 CI 上会静默跳过 = 没测），测试自建最小 `dict.sqlite`（只 `dict` 表 + 一条词条），因此该用例在任何环境都真的执行
- [x] 4.2 反向验证（**这条是本任务的价值所在**）：临时把至少 3 处（必须含侧栏图标、快捷键提示文字、收藏星标）改回 `scheme.outline`，确认测试**转红并打印出具体对比度数值**（约 1.2~1.3:1）；然后恢复；验证：两种状态各跑一次，把红/绿结果写在勾选说明里 → **实测：先红后绿，两向都验过**。**红**：用 `sed` 把 3 处改回 `outline`（`app_shell.dart:192` 导航图标、`:204` 快捷键提示、`search_page.dart:835` 星标），4 个用例转红并**逐条打印**：浅色 `#E4E2DD` on `#F7F6F3` = **1.20:1**（4 个导航图标 + `Ctrl+B/E/I/R`）、浅色 `#E4E2DD` on `#FFFFFF` = **1.29:1**（星标）；深色 `#33362F` on `#1C1E1C` = **1.37:1**、深色 `#33362F` on `#242724` = **1.23:1**（星标）—— 与 design 记录的 1.2~1.3:1 吸合。**绿**：按字节还原（`md5sum` 与备份**完全相同**：`73667c92…` / `d411cc4a…`），再跑 **13/13 全过**；并确认全仓 `scheme.outline\|colorScheme.outline` 又只剩 2 条注释。**额外一向**（D5 的登记制也验了会红）：往 `_exceptionsFor` 注入一条永远不会命中的假例外 E9 → 「允许清单不得腐烂」用例转红（`Expected [E1,E2,E3,E4,E9] / Actual [E1,E2,E3,E4]`），且 12 个其余用例仍绿（说明它是“多出的豁免”而不是“真违例”）；注入已还原
- [x] 4.3 若 4.1 的断言依赖硬编码背景色，改为从主题取值（避免色值漂移导致测试与实现脱节）；验证：`grep -c "Color(0x" test/ui_contrast_test.dart` 为 0（只允许引用 `LupaColors.*`） → **实测：`grep -c "Color(0x" test/ui_contrast_test.dart` = 0**。背景色全部来自**主题取值**而非魔数：底色用 `theme.scaffoldBackgroundColor` / `theme.colorScheme.surface` / `theme.colorScheme.surfaceContainerHighest`，例外清单用 `LupaColors.tx3Light` / `bgLight` / `jade` / `jadeSoftLight`；另有一条断言把 `scheme.outline` 钉在 `LupaColors.borderLight/borderDark` 上，防止实现与「outline = 描边色」这个前提脱节（也就防止了本 change 的核心假设静默失效）

## 5. 集成验证

- [x] 5.1 依次运行 `flutter analyze`、`flutter test`；验证：前者无新增 error/warning，后者全过（当前基线 292 个用例） → **实测：`flutter analyze` 18 条，与 HEAD 基线*逐条完全相同***（不只看总数：临时 `git worktree add` 一份未含本次改动的 HEAD 到 `/tmp/lupa_base`，两边各跑 analyze 后把「规则 → 文件:行:列」排序 diff → 无差异，无新增也无消失）；`flutter test` **308/308 全过** = 基线 292 + 侧栏 3 + 对比度 13。（注：`flutter test` 需带 `LUPA_HOME`，否则既有的 `ai_search_overflow_test.dart` 会在 `Platform.environment['LUPA_HOME']!` 上抛空断言 —— 已核实是**既有**环境依赖，与本次改动无关）
- [x] 5.2 运行八组 `verify_*.dart`（`verify_notebook_db` / `verify_dict_query` / `verify_notebook_repo` / `verify_media` / `verify_export` / `verify_e2e` / `verify_phrase_repo` / `verify_ai`）；验证：全部 0 失败 —— 证明纯界面改动没有触碰数据层 → **实测：八组全部 exit=0、共 307 条 PASS、0 FAIL**：`notebook_db` 8 PASS；`dict_query` 17 PASS（`ALL PASS`）；`notebook_repo` 64（`ALL PASS`）；`media` 19（`ALL PASS`）；`export` 26（`ALL PASS`）；`e2e` 15（`== 15 passed, 0 failed ==`）；`phrase_repo` 77（`ALL PASS`）；`ai` 81（`PASS 80 / FAIL 0`，stub 模式、不触网、不需密钥）。数据层未动，符合预期
- [x] 5.3 手动看一眼实机观感：浅色与深色主题下侧栏（选中/未选中态）、三个空状态页、查词页未收藏星标；确认可读性提升且无「过重」的观感问题；验证：若装饰性空状态图标过重，按 design 的 Open Question 单独取值（仍 ≥3:1），并在勾选说明里记录最终选择 → **实测：用「真字体截图」跑的目视检查**（临时脚本注册 Windows `NotoSansSC-VF.ttf` / `georgia.ttf` / MaterialIcons 真实字体后用 `matchesGoldenFile --update-goldens` 落成 10 张 PNG，看完即删；不能用默认测试字体 —— 它把每个字形画成方块，对观感毫无参考价值）。**观察结果**：（1）**改后**浅/深侧栏：5 个未选中导航图标、快捷键提示、底部「设置」图标均清晰可辨；选中态（玉青图标+文字+浅玉底）层次清楚；（2）**改前对照图**（把 7 处临时改回 `outline` 再渲一次）：未选中导航图标、`Ctrl+B/R/I/E` 提示、底部「设置」图标、空状态大图标**全部呈幽灵般几乎不可见** —— 直观坐实了 1.2~1.3:1 的实际后果，也解释了为何「图标看不见 = 功能不可发现」；（3）**矮窗口 300px**：滚动后「设置」入口完整可见可点，且侧栏没有任何被裁剩的残影；（4）**关于 design 的 Open Question「装饰性空状态图标会不会过重」→ 最终选择：保持统一用 `onSurfaceVariant`（8.63:1），不另取中间值**。依据：目视 44px（查词）与 48px（生词本/短语集）两个尺寸，它们读起来是「恰当的视觉锚点」，与下方 titleLarge 标题构成清晰的主次关系，没有压过标题、也没有变“实心重块”（都是线性描边图标）；反过来说，改前那种「看不见」才是真问题。（5）顺带目视复核了 E2/E4 两处已登记例外：「词频 #1000」与「今日到期」是各自页面上**最浅**的文字，与 4.17 / 4.23:1 的实测吸合 —— 但仍在可读区间，故同意按原计划顺延。临时脚本与 PNG 已全部删除，`git status` 无多余文件
